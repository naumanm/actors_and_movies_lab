<h1>Add a new movie</h1>

<%= form_for @movie do |f| %>
  <%= f.text_field :title, :placeholder => "Title" %>
  <%= f.text_field :year, :placeholder => "Year" %>

    <%= f.collection_select(:id, Actor.all, :id, :name, prompt: "Who are you?" ) %>

  <%= f.submit "Add" %>
<% end %>

</p>
